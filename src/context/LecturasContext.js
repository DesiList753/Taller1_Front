// Este simplemente lo hago por que el lint me estuvo webeando mucho que el fast refresh solo funciona cuando exporta componentes
import React from 'react';

const LecturasContext = React.createContext();

export default LecturasContext;
